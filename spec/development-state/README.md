# Development State

These JSON files are the repository-resident source of truth for implementation program state. Chat and PR comments are coordination surfaces only.

`governance-model.json` contains invariants and authority rules.
`frontier-state.json` identifies dependency-eligible work.
`program-state.json` records the current program lifecycle position.

State changes must occur through the governance process and be reviewed as authoritative changes.
