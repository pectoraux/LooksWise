# Provenance

Every output artifact preserves a chain:

```text
source media
→ AppearanceState version
→ Look version
→ Recipe version
→ capability implementation(s)
→ provider/model metadata
→ execution identity
→ evaluation
→ output artifact
```

External exports should support compatible content provenance standards where practical. Provenance does not override privacy/consent restrictions.
